public class SwapElements {
    
    public static void swapElement(int[] arr, int index, int element){

        // just write the element in the point of index
        arr[index]= element;

    }
    
}
